# TossTrading — 구현 노트 (v0.2)

> 설계 문서(01)의 Phase 0~3 범위를 구현한 1차 버전에 대한 기록입니다.
> 무엇이 구현·검증되었고, 무엇을 실제 토스 계정으로 확인해야 하는지 정리합니다.

## 1. 구현 범위

| 설계 문서 항목 | 상태 | 위치 |
|---|---|---|
| 호가단위 / 비용 모델 / 비용 가드 | ✅ | `Domain/TickRules.cs`, `Domain/CostModel.cs`, 봇 설정창 [비용 점검] |
| 리스크 기반 포지션 사이징 | ✅ | `Domain/PositionSizer.cs` |
| 토스 OAuth 토큰(선제 갱신, 401 재시도) | ✅ | `Toss/TossRestClient.cs` (`TossTokenProvider`) |
| REST 그룹별 호출 한도 + 429 Retry-After + `X-RateLimit-Limit` 자동 보정 | ✅ | `Toss/TossRestClient.cs`, `Domain/RateGate.cs` |
| 웹소켓: 선언형 구독, 텍스트 PING, 지수 백오프 재연결, 거부 처리 | ✅ | `Toss/TossStreamClient.cs` |
| 주문 이벤트 누락 대비 REST 워치독 | ✅ | `Toss/TossAdapters.cs` (`TossBroker`) |
| 스캐너 (랭킹 합집합 → 필터 → RVOL → 점수) | ✅ | `Engine/Scanning/ScannerService.cs` |
| 1분봉·VWAP·ORB·ATR·체결강도, 늦은 시작 시 분봉 복원 | ✅ | `Engine/Market/SymbolContext.cs` |
| 진입 전략: ORB / VWAP 눌림 / 박스 고가 돌파 / 수동 | ✅ | `Engine/Strategies/EntrySignals.cs` |
| 봇 상태머신, 청산 규칙(손절·본절·분할익절·트레일링·타임스탑·강제청산) | ✅ | `Engine/Trading/TradingBot.cs` |
| 목표 도달 시 정지 3단계 | ✅ | 봇(거래/누적), `Engine/Risk/RiskManager.cs`(계좌) |
| 주문 우선순위 큐 (손절 > 취소·정정 > 익절 > 진입), 개장 10분 한도 축소, 멱등 재전송 | ✅ | `Engine/Trading/OrderManager.cs` |
| 정정 시 새 orderId 추적, ack 보다 먼저 온 체결 이벤트 버퍼링 | ✅ | `Engine/TradingEngine.cs` |
| 재연결 후 계좌 재동기화 → 봇 재개 | ✅ | `TradingEngine.ReconcileAsync` |
| 킬스위치 | ✅ | 엔진 + UI |
| 페이퍼 브로커 (호가 걸어 올라가기, 보수적 대기 체결) | ✅ | `Engine/Paper/PaperBroker.cs` |
| 시뮬레이션 시장 (API 키 없이 체험/테스트) | ✅ | `Engine/Simulation/SimulatedMarket.cs` |
| 거래 기록(JSONL), 로그, 틱/호가 기록(gzip) | ✅ | `Engine/Infrastructure/EngineLog.cs` |
| WPF UI (대시보드·스캐너·봇·차트·거래·로그·설정) — DevExpress 24.1.7 (ThemedWindow, GridControl, ChartControl, DevExpress.Mvvm) | ✅ | `App/` |
| 리플레이 백테스터 / 리포트 화면 | ⏳ 다음 단계 | 틱 기록은 이미 쌓이도록 구현됨 |
| 서버측 안전망(조건주문) | ⏳ 다음 단계 | 조건주문 동작 확인 후 |
| 미국 주식 모드 | ⏳ 다음 단계 | 도메인은 `MarketCountry.US` 호가단위 준비됨 |

## 2. 설계 대비 달라진 점

- **스레딩**: 설계 문서 8.4는 "봇마다 Actor"였지만, 구현은 **엔진 전체를 단일 이벤트 루프**로 직렬화했습니다
  (시세·주문 이벤트·사용자 명령·타이머 → 하나의 `Channel<Action>`). 수십 종목 규모에서는 성능 여유가 충분하고,
  봇·리스크·주문 추적 사이에 락이 전혀 없어 경쟁 조건이 원천적으로 사라집니다. 주문 전송(REST)과 스캐너만 별도 스레드입니다.
- **웹소켓 연결 1개**: 계정당 2개 제한 중 1개만 사용 (봇 종목 체결+호가, 스캐너 상위 후보 체결, 내 주문 이벤트 → 95토픽 예산).
  나머지 1개는 토스 앱/다른 도구용 여유로 남겼습니다.
- **기본 리스크**: 거래당 리스크 기본값을 0.5% → **0.2%** 로 낮췄습니다. 봇 손실 한도(최대 투입금의 2%)와 맞추기 위해서입니다.

## 3. 검증한 것 (이 저장소 안에서)

- `dotnet test` — **56개 테스트 통과** (반복 실행 안정)
  - 호가단위 경계(1,999→2,000→2,005, 5,000−1틱=4,995 등), 왕복비용 0.43%(1만원), 순손익·본절가
  - 사이징(설계 문서 7.1 예시 250주), 리스크 한도·연속손실 쿨다운·킬스위치
  - 봇: 수동매수→손절, 분할익절→트레일링→봇 목표 달성 완료, 진입 타임아웃, 매도 미체결→시장가 정정, 타임스탑, 킬스위치
  - 페이퍼: 호가 걸어 올라가기, 같은 가격 대기 체결 금지, 과매도/예수금 부족/호가단위 위반 거부, 정정 시 새 주문ID
  - 엔진 통합: 시뮬레이션 + 페이퍼로 매수 → 킬스위치 → 전량 청산
  - 토스: 결과 봉투/문자열 숫자 파싱, 계좌 헤더, 주문 본문(숫자=문자열), 에러 봉투 → 예외, 만료 토큰 1회 재발급, 403 IP 안내,
    웹소켓 선언 JSON 형식, 체결/호가/주문 프레임 파싱
- `TossTrading.Cli sim` — 헤드리스로 스캐너→봇 추가→자동 진입→분할익절/트레일링/손절/봇 손실한도/최대 진입 소진까지 전 과정 동작 확인
- WPF 앱 — DevExpress 24.1.7 은 공개 NuGet 에 없어서, 개발 환경에서는 같은 API 의 **DevExpress 25.1.14 로 컴파일 검증**(XAML 포함, 오류 0)했습니다.
  그리드 필드명·바인딩 경로도 정적 점검했습니다. **24.1.7 로컬 패키지로의 빌드와 실행 화면은 Windows 에서 확인 필요** (개발 환경이 Linux)

## 4. 실제 토스 계정으로 확인해야 하는 것 (★)

개발 환경에서 `developers.tossinvest.com` 접근이 막혀, API 형식은 **비공식 SDK(toss-go v0.3.0) 소스**를 기준으로 구현했습니다.
`TossTrading.Cli check` 로 아래를 먼저 확인하세요.

1. 토큰 발급 형식 (`POST /oauth2/token`, form-urlencoded) — `check` 1단계
2. 응답 봉투 `{"result": ...}` 와 필드명 — `check` 의 현재가/호가/분봉/랭킹/종목정보 단계
3. 계좌 헤더 `X-Tossinvest-Account` — `check` 의 매수가능금액/보유종목 단계
4. 웹소켓 선언 형식과 프레임 — `check` 마지막 15초 수신 (장 시간에 실행)
5. 주문 API — **반드시 소액·지정가·체결 안 될 가격**으로 1회 실험 후 취소 (앱의 실전 모드 전에)
6. 호출 한도 수치 — 응답 헤더 `X-RateLimit-Limit` 을 자동 반영하지만 초기값은 `TossOptions` 에서 조정
7. mTLS 인증서 필요 여부 — 필요하다면 `TossRestClient` 의 `HttpClient` 에 클라이언트 인증서를 추가해야 함

형식이 다르면 수정 지점은 `src/TossTrading.Toss/Dtos.cs`(필드명)와 `TossRestClient.cs`/`TossStreamClient.cs` 두 곳으로 한정됩니다.

## 5. 다음 단계 제안

1. **Phase 0 실행**: `check` 결과 공유 → 형식 차이 수정
2. 토스 실시간 + 모의 주문으로 매일 운용 → 틱 기록 축적
3. 리플레이 백테스터 (`ticks/*.jsonl.gz` → `SimulatedMarket` 대체 피드) + 전략별 리포트 화면
4. 조건주문 기반 서버측 안전망, 텔레그램 알림

## 6. 종가매매 (종가 베팅 → 익일 매도)

프리셋 **"종가베팅 (익일 매도)"** 또는 봇 설정 ⑤에서 사용합니다.

**진입 (`ClosingBetSignal`, 기본 15:00~15:19, 하루 1회)** — 모두 만족 시 신호
1. 당일 등락률 +3% ~ +20% (강세지만 상한가 추격은 아님)
2. 당일 고저 범위 상단 75% 이상에서 거래 (고가 부근 마감)
3. VWAP 위, 체결강도 100 이상(데이터 있을 때)
4. 최근 30분간 밀리지 않음
5. 상한가 3% 이내 제외

**보유·청산 규칙 (`HoldOvernight`)**
- 당일 강제청산(15:10)·타임스탑을 쓰지 않는다. 신규 진입은 15:20(종가 단일가 시작) 전까지만.
- 진입 당일에는 **손절만** 적용 (익일 갭을 노리는 전략이므로 당일 익절·트레일링은 하지 않음).
- 정규장 접속매매(09:00~15:20)에서만 청산 판단 — 동시호가·NXT 시간외의 얇은 체결로 손절되지 않도록.
- 익일 청산 방식
  - **시초 매도**: 익일 첫 체결 직후 전량 시장가
  - **관리**(기본): 손절·분할익절·트레일링·본절을 적용하고, 익일 청산 시각(기본 10:00)에 남은 수량 매도
- 프리셋 기본값: 손절 3%, 1차 익절 +2%(50%), 최종 +5%, 트레일링 +2.5% 이후 고점 대비 1.5%, 거래당 리스크 0.2%, 1회 진입

**재시작 후 이어서 관리 (토스 데이터일 때)**
- 봇 상태와 모의계좌(예수금·보유)를 `%LocalAppData%\TossTrading\state\` 에 2초마다(변경 시) 저장
  (`bots_paper.json` / `bots_live.json`, `paper_account.json`).
- 다음 날 시작하면 보유 중인 봇만 복원해 바로 청산 관리를 시작하고, 당일 통계는 새로 시작한다. 같은 날 재시작하면 모든 봇을 복원.
- 실전 모드는 복원 시 실제 계좌 잔고와 대조해 수량을 맞춘다. 계좌에 남은 미체결 주문은 경고만 한다.
- 설정 → 비용·기타 → **모의계좌 초기화**로 저장된 모의 상태를 지울 수 있다.
- 시뮬레이션 시장은 종목이 매번 새로 만들어지므로 저장하지 않는다. 대신 **시뮬레이션 시작 시각**을 14:30 등으로 두면 종가 진입까지는 바로 연습할 수 있다
  (익일 청산은 토스 실시간 + 모의 주문으로 확인).

**주의**
- 밤사이 악재·시장 급락으로 **익일 시가가 손절가보다 훨씬 낮게 시작할 수 있다**. 손절은 시가 이후에 나가므로 손실이 설정 %보다 커질 수 있다 → 투입금·리스크 %를 보수적으로.
- 종가매매 효과는 시장 국면에 따라 크게 달라진다. 반드시 모의로 20거래일 이상 검증한 뒤 소액으로.
- 헤드리스 확인: `dotnet run --project src\TossTrading.Cli -- sim 70 60 closing` (14:40 시작, 종가베팅 봇 자동 운용)

### 6-1. 종가매매 후보 검색 (스캐너 "종가매매 후보" 모드)

스캐너 위 **모드**를 `종가매매 후보`로 바꾸면:
- 필터: 등락률은 종가매매 범위(기본 +3~20%)로, RVOL 조건은 빼고(거래대금으로 유동성 판단) 나머지(가격·틱비용·경고·보통주)는 동일.
- **후보 전체**의 당일 1분봉을 조회해 고저 범위 위치·VWAP·30분 추세를 계산 (실시간 구독 상위 20개에 한정되지 않음).
  호출 한도를 위해 한 번 스캔에 8종목씩(`ClosingBarsPerCycle`), 60초 지난 것부터 갱신 → 후보 40개 기준 약 25초면 전체가 채워진다.
  실시간 구독 중인 종목은 실시간 값(체결강도 포함)을 우선 사용.
- 각 종목에 봇 매수 조건과 같은 기준의 **조건표**: `등락✔ 고가권✔ VWAP✔ 30분✖ 강도? 상한가✔` (✔ 통과 ✖ 실패 ? 데이터 없음)
- 정렬: 조건 모두 통과(태그 `종가후보`, 굵게) → 통과 개수 → 종가 점수
  (고가권 30% · VWAP 20% · 30분 추세 20% · 거래대금 15% · 체결강도 15%, 실패 항목당 −10점)
- 모드를 바꾸면 봇 추가 프리셋이 자동으로 "종가베팅 (익일 매도)"로 바뀐다.
- 체결강도는 실시간 구독 종목만 계산되므로 하위 후보는 `강도?` 로 표시된다. 봇을 추가하면 해당 종목을 구독하므로 봇이 매수 전 체결강도까지 다시 확인한다.

## 7. 알고리즘 개선용 매매 로그와 성과 분석

목표: "어떤 조건에서 들어간 거래가 돈을 벌었나 / 손절·익절 폭이 맞나 / 놓친 기회는 없나"를 **데이터로** 답할 수 있게 한다.

### 7-1. 저장되는 기록 — `%LocalAppData%\TossTrading\journal\analysis_yyyyMMdd.jsonl`
한 줄에 한 건, `{"kind": ..., "data": {...}}` 형식 (한글 그대로 저장 → 메모장에서도 읽힘).

| kind | 언제 | 주요 내용 |
|---|---|---|
| `trade` | 거래 종료(전량 매도) 시 | 진입 근거, **진입 시 시장 상황**(등락률·VWAP 이격·당일 범위 위치·체결강도·ATR%·거래량 배수·스프레드·장 시작 후 분), 결정가 대비 **진입 슬리피지**, 초기 손절·손절 거리%, **MFE/MAE**(보유 중 최고/최저), 매도 체결 내역(분할 익절 포함, 분류·기준가·체결가), **청산 슬리피지**, 총손익·비용·순손익·R, 보유 시간, 익일 보유 여부, **당시 봇 설정 전체**, 청산 시 시장 상황 |
| `signal` | 전략 신호 발생 시 | 결과(진입 / 차단+사유 / 승인대기 / 시간외 / 진입불가), 신호 근거, 시장 상황. 같은 결과가 반복되면 60초에 1회만 |
| `decision` | 반자동 신호 처리 시 | 승인→진입, 승인→차단, 거절, 만료(60초/가격 이탈) |
| `followup` | 청산·미진입 신호 이후 | 5/15/30/60분 후 가격, 30·60분 최고/최저, 장 마감가 → "손절 후 반등했나", "익절 후 더 갔나", "안 들어간 신호는 어땠나" |

- 진행 중인 거래의 분석 상태는 봇 상태(`bots_*.json`)와 함께 저장되어 **종가매매 익일 보유/재시작 후에도 이어서** 기록된다.
- 추적 중인 종목은 봇을 지워도 60분간 체결 구독을 유지한다 (구독 한도 내).
- 기존 `trades_yyyyMMdd.jsonl`(요약)과 `logs\log_yyyyMMdd.txt`(텍스트 로그)는 그대로 유지.

### 7-2. 분석 — 앱 [📊 성과 분석] 또는 CLI `report`
- 기간(오늘/5/30/90일/전체), 데이터(전체/토스/시뮬) 필터.
- 리포트: 요약(승률·기대값·손익비·PF·평균 R·최대 연속 손실), 전략별, 청산 사유별, 진입 시간대별, 진입 조건 구간별(등락률·VWAP 이격·범위 위치·체결강도·거래량 배수·스프레드), MFE/MAE 점검, 청산 후 흐름, 슬리피지, 신호 처리 결과.
- **개선 제안** (구간당 5건 이상일 때만): 예)
  - 손실 거래 다수가 한때 +1R → 본절 이동을 앞당기거나 분할 익절 확대
  - 이익 거래의 MAE 90% 지점이 손절 거리보다 훨씬 작음 → 손절 폭 축소 검토
  - 손절 후 60분 안에 매수가 회복이 잦음 → 손절이 너무 타이트 / 구조적 손절 사용
  - 익절 후 +2% 이상 추가 상승이 잦음 → 목표 상향·트레일링 연장
  - 특정 조건 구간의 기대값이 음수 → 진입 필터로 제외
  - 진입 슬리피지 과다 → 슬리피지 틱 축소, 스프레드 넓은 종목 제외
- [리포트·CSV 저장] → `reports\report_*.md`, `trades_*.csv`, `signals_*.csv` (UTF-8 BOM, 엑셀에서 바로 열림) → 피벗 테이블로 추가 분석.

```
dotnet run --project src/TossTrading.Cli -- report 30                 # 최근 30일, 기본 데이터 폴더
dotnet run --project src/TossTrading.Cli -- report 5 --source toss    # 토스 실데이터만
dotnet run --project src/TossTrading.Cli -- sim 120 120 --data .\simdata   # 시뮬 결과도 기록해 분석해볼 수 있음
```

### 7-3. 개선 절차 권장
1. 모의투자로 최소 30~50건 이상 쌓는다 (시뮬레이션 데이터는 토스 실데이터와 분리해서 볼 것).
2. 리포트의 제안 중 **하나만** 골라 설정(프리셋)을 바꾼다 — 기록에 당시 설정이 함께 저장되므로 전후 비교 가능.
3. 같은 기간 이상 다시 돌린 뒤 전략/설정별 기대값·PF를 비교한다. 표본이 적을 때의 차이는 우연일 수 있다.

## 8. 자동 운용 (종목 자동 선정 → 단타 → 종가매매 자동 전환)

메인 화면 **[🤖 자동 운용]** 체크 → 엔진이 시간대별로 종목 선정·봇 생성·정리를 모두 자동으로 한다 (`Engine/Automation/AutoPilot.cs`).

| 시각 (기본값) | 동작 |
|---|---|
| ~09:05 | 대기. 전날 종가 보유분은 각 봇이 익일 청산 규칙대로 매도 |
| 09:05 ~ 14:30 | 스캐너 **단타 모드**. 점수 50 이상 상위 종목으로 **단타 봇 최대 3개** (완전자동). 10:00 전에는 "ORB 표준", 이후 "VWAP 눌림 표준" 프리셋 |
| 〃 | 봇이 끝나면(목표 달성·손실 한도·진입 소진) 목록에서 빼고 **다른 종목으로 채움**. 30분간 진입이 없고 상위 후보에서 밀려난 봇도 교체. 같은 날 같은 종목은 다시 고르지 않음 |
| 14:30 | 단타 신규 진입 마감 (보유분은 계속 관리) |
| 14:40 | 스캐너 → **종가매매 후보 모드** (분봉 수집 시간 확보) |
| 14:50 | **단타 보유분 전량 정리** (종가 자금 확보) |
| 14:55 ~ 15:15 | 종가 조건을 **모두 통과**한 상위 종목으로 **종가베팅 봇 최대 2개**. 조건에서 3회 연속 탈락한 미진입 봇은 교체 |
| 15:00 ~ 15:19 | 종가베팅 전략이 조건을 다시 확인하고 매수 |
| 15:20 이후 | 매수 못 한 종가 봇 정리. 보유분은 다음 날 매도 후 자동 정리 |

- 킬스위치·일 손실 한도·연속 손실 쿨다운 중에는 **새 종목을 고르지 않는다** (기존 봇의 청산 관리는 계속).
- 봇 목록 "구분" 열: 🤖 단타 / 🤖 종가 (이전 버전에서 넘어온 봇은 "기존").
- 자동 운용을 끄면 새 종목만 고르지 않는다. 스캐너 모드는 계속 시간대대로 바뀌고, 보유 종목은 각 봇이 규칙대로 관리하며, 끝난 봇은 목록에서 자동 정리된다.
- 설정: [설정 → 자동 운용] (시각, 봇 수, 최소 점수, 프리셋, 교체 시간, 종가 조건 최소 통과 수).
- 익일 보유 종목도 [리스크 → 동시 보유 한도](기본 4)에 포함된다. 오전에는 (전날 종가 보유 + 단타)가 한도를 넘으면 일부 단타 진입이 막힐 수 있다.
- CLI 확인: `dotnet run --project src/TossTrading.Cli -- sim 240 120 auto` (가상 09:00~15:30 하루 전체).

## 9. 백테스트 (기간을 정해 과거 데이터로 자동 운용 재생)

메인 화면 **[📈 백테스트]** → 기간·데이터·시작 자금 → [▶ 실행]. CLI: `backtest 2026-08-01 2026-08-31 --source toss`.

**구조** (`Engine/Backtest/`)
- `BacktestRunner`: 실매매와 **같은 TradingEngine**(자동 운용·봇·리스크·모의체결·분석 기록)을 가상 시계로 돌린다. 매 단계 `SettleAsync`로 주문·이벤트 처리가 끝날 때까지 기다려 결과가 매번 같다(결정적).
- `ReplayMarket`: 1분봉을 15초 간격 4개 체결로 분해 (양봉 시가→저가→고가→종가, 음봉 시가→고가→저가→종가 — 손절이 먼저 걸리는 보수적 경로). 호가는 1틱 스프레드로 합성. 순위·분봉·일봉 조회는 **그 시각까지의 데이터만** 돌려준다 (미래 참조 없음). 스캐너는 실시간과 같은 `ScanOnceAsync`를 1분마다 실행.
- 날짜별 종목 선별: 그날 일봉 **고가** 기준 등락률 ≥ 하한, 거래대금 ≥ 기준의 30% (장중 후보가 될 수 있었던 필요조건) → 거래대금 상위 N개 + 전날부터 보유 중인 종목.
- 데이터: `TossHistoryProvider`(토스 `candles` API, `before` 페이지 조회) → `CachedHistoryProvider`(`%LocalAppData%\TossTrading\history\`, 지난 날짜는 다시 조회 안 함). `SyntheticHistoryProvider`는 API 키 없이 동작 확인용 가상 데이터(무작위라 전략 성과 판단에는 쓰지 말 것).
- 결과: `backtests\<실행시각>\` 에 `report.md`(요약·전략별·일별·거래 내역), `trades.csv`, `daily.csv`, `analysis.md`([성과 분석]과 같은 진입조건·MFE/MAE·개선 제안), `journal\`, `logs\`.

**한계** (리포트에도 표시)
- 대상 종목 = **코스피·코스닥 전체 상장 종목** (`GET /api/v1/stocks/all`, STOCK_ALL 1/s). 처음 실행 때 전 종목 일봉(약 2,500회 × 캔들 한도 15/s ≈ 3분)을 받아 캐시하고, 날짜별로 그날 후보가 될 수 있었던 종목만 분봉을 받는다.
  - 순위 API 에는 조회 시점 파라미터가 없다 (항상 "지금" 기준). "그 시점의 순위"는 분봉 누적 거래대금·등락률로 매 분 다시 계산하고, 실시간과 같이 유형별 상위 100개만 스캐너에 보인다.
  - 전체 목록 조회가 안 되면 순위(현재 + 백테스트 기간을 덮는 1w~1y 집계) 상위 종목으로 대신한다.
  - 기간 중 상장폐지된 종목은 빠진다 (생존 편향, 짧은 기간에서는 영향 작음).
- 호출 한도 그룹을 공식 문서 기준으로 분리: 캔들 MARKET_DATA_CHART, 순위 RANKING, 전체 종목 STOCK_ALL.
- 토스가 몇 달 전까지의 1분봉을 주는지는 실제 계정으로 확인 필요. 없으면 그날은 "분봉 데이터 없음"으로 건너뛴다.
- 분봉 내부 체결 순서·호가 잔량·체결강도·VI·투자경고는 재현하지 않는다.

**함께 고친 것**: 엔진을 밤새 켜 두면 종목별 당일 통계(VWAP·고저·분봉)가 다음 날로 넘어가던 문제 → 날짜가 바뀌면 `SymbolContext.ResetSession()`으로 초기화하고 상하한가·전일종가를 다시 채운다.

## 10. 자동 선정 중심으로 정리 (수동 기능 제거)

종목 자동 선정이 기본이 되면서 화면에서 뺀 것:

| 제거 | 대체 |
|---|---|
| 스캐너 [모드] 선택 | 시간대에 따라 자동 전환 (09:00~ 단타 후보, 14:40~ 종가매매 후보). 자동 운용이 꺼져 있어도 전환 |
| [＋ 선택 종목 봇 추가] · [코드로 추가] · 프리셋 선택 | 🤖 자동 운용이 후보 상위 종목으로 봇 생성 |
| 봇 [▶ 시작] · [수동 매수] · 신호 [✔ 승인]/[거절] | 자동 운용 봇은 완전자동 (신호 → 즉시 매수) |
| 봇 [삭제] | 끝난 봇(보유·미체결 없음)은 자동으로 목록에서 정리 |
| [프리셋으로 저장] · 봇 설정의 "운용 모드" | ⚙ 설정 → 자동 운용 → 오전/장중/종가 프리셋 옆 **[편집]** |
| 기본 프리셋 "수동진입·자동청산" | 삭제. 기존 설정 파일의 수동 전략 프리셋도 불러올 때 정리되고 나머지는 완전자동으로 맞춤 |

남긴 것: 봇 [청산] · [정지(청산)] · [설정 변경], 🤖 자동 운용 켜기/끄기(기본 켜짐), ⛔ 킬스위치.
엔진의 수동 진입·반자동 기능 자체는 테스트와 CLI 에서 쓰므로 코드에는 남아 있다.

### 9-1. 재생 정밀도 · 진단 기록 (실데이터 1차 결과 반영)

2026-01~09 토스 데이터 백테스트(1,146건, -66%)를 분석한 결과, 손실의 상당 부분이 **재생 방식의 한계**였다.
- 1분봉을 꼭짓점 4개(시가·저가·고가·종가)로만 재생 → 가격이 손절가를 **뛰어넘어** 봉 저가에서 체결 (손절 체결 중앙값 -0.24%, 하위 10% -1.3%, 최악 -13%)
- 돌파 전략은 돌파 지점이 아니라 **봉 고가**에서 매수 → ORB 보유시간 중앙값 2.9분, 53%가 5분 안에 손절
- 조치: 구간(시가→저/고가→고/저가→종가)마다 **중간 가격 6개**를 채워 재생 (`BacktestOptions.TicksPerLeg`, 체결마다 엔진 처리 완료 후 다음 가격). 가상 데이터 기준 손절 체결 평균 -0.11% → -0.04%
- 엔진 대기 루프가 `Task.Delay(1)`(Windows 약 15ms)로 쉬던 것을 양보(Yield) 위주로 바꿔 속도 개선

백테스트 결과 폴더에 추가된 파일:
| 파일 | 내용 |
|---|---|
| `options.json` | 이번 실행에 쓴 설정 전체 (자동 운용·프리셋·리스크·스캐너·비용) |
| `execution.md` | 청산 유형별 체결가(기준가 대비), 전략별 보유시간·5분 내 손절 비율, 익일 갭 손절 |
| `trades_detail.csv` | 거래별 진입 조건·MFE/MAE·슬리피지·당시 설정 (엑셀) |
| `signals.csv` | 신호별 처리 결과(진입/차단 사유)와 이후 5~60분 가격 |
| `trade_bars.jsonl` | 거래별 진입 10분 전 ~ 청산 10분 후 1분봉 (체결 검증용) |

### 9-2. 실데이터 2차 결과 (정밀 재생) 분석과 수정

2026-01~09, 1,203건, -39.9% (1차 -66%에서 개선 — 재생 방식 수정 효과). 전략별:

| 전략 | 거래 | PF | 거래당 | 판단 |
|---|---:|---:|---:|---|
| VWAP 눌림 | 416 | 0.34 | -0.48% | 상·하반기 모두 PF 0.3 대 → **기본 끔** (장중 프리셋 "(사용 안 함)") |
| ORB | 544 | 0.77 | -0.22% | 버그 2개 발견 (아래) |
| 종가베팅 | 243 | 0.93 | -0.18% | 본전 근처. 익일 갭 손절 100건 평균 -3.95% |

발견한 버그:
1. **자동 운용이 프리셋의 진입 시간대를 14:30 으로 덮어씀** → ORB(11:00 종료)가 오후까지 아침 범위 "돌파"를 매수 (예: 13:36 진입). → 프리셋 시간을 넓히지 않도록 수정.
2. **ORB 가 되밀린 뒤 같은 가격 재돌파를 새 돌파로 봄** (0.3% 아래로 내려가면 재무장). → **하루 첫 돌파만** 신호, 봇이 생기기 전 이미 돌파한 종목도 제외. 자동 운용 ORB 봇은 1회 거래 후 종료해 다른 종목에 자리를 넘김.

2차 결과 거래에 위 조건을 적용해 추정하면 약 -0.7M(-7%). 거래당 -0.18% 는 왕복 비용(≈0.23%)과 거의 같다 → **비용 전 기준으로는 우위가 거의 없음**. 다음은 거래 수를 줄이고 한 번에 먹는 폭을 키우는 방향(목표·트레일링 폭, 진입 필터)을 설정별로 비교해야 한다.

추가 기능: 봉 내부 순서 가정 선택(보수적 / 시가에서 가까운 쪽 먼저, CLI `--path nearest`) — 두 결과 차이가 크면 1분봉 백테스트로는 판단하기 어려운 전략이다.

### 9-3. 연구용 데이터 내보내기

거래 로그에는 실제로 한 거래만 남아 "다른 규칙이었다면?"을 시험할 수 없다. 백테스트 때 받은 과거 데이터(캐시)를 묶어 올리면 같은 데이터로 전략·설정을 반복 비교할 수 있다.
- 앱: 백테스트 창 **[📦 연구용 데이터 내보내기]** → `%LocalAppData%\TossTrading\research_export\`
- CLI: `export-data [--out 폴더]`, 재생: `backtest <시작> <종료> --source export:<폴더>` (네트워크 불필요)
- 형식: `universe.csv`, `daily.csv.gz`, `minute_yyyyMM.csv.gz` (정규장 1분봉만, gzip). 내보낸 데이터로 돌린 백테스트는 원본 캐시와 결과가 같다(테스트로 확인).
- 종목명은 이번 버전부터 캐시에 `universe.json` 으로 저장된다 (이전 캐시는 코드만).

3차 결과(단타=ORB 오전만): 보수적 -7.9%, 가까운쪽먼저 -6.6% — 봉 내부 가정에 크게 좌우되지 않음. 거래당 -0.15% 로 왕복 비용 수준.
익절·트레일링 청산의 64%가 이후 60분 안에 +2% 이상 더 올랐다(평균 최고 +4.1%) → 청산이 이르다는 신호. 원본 데이터로 검증 예정.

### 9-4. 기본값 확대 (봇·종목 수)

| 항목 | 이전 | 현재 |
|---|---:|---:|
| 동시 단타 봇 | 3 | 6 |
| 동시 종가 봇 | 2 | 4 |
| 미진입 봇 교체 | 30분 | 20분 |
| 동시 보유 한도 | 4 | 10 |
| 총 노출 한도 | 60% | 90% |
| 일 손실 한도 | 1.5% | 2.0% |
| 연속 손실 쿨다운 기준 | 4회 | 6회 |
| 스캐너 후보 / 실시간 구독 | 30 / 20 | 40 / 30 |
| 백테스트 하루 최대 종목 | 40 | 60 |

기존 설정 파일은 이전 기본값 그대로인 항목만 새 값으로 옮긴다 (SettingsVersion 3).
3차 결과 기준 두 전략 모두 거래당 약 -0.15% (상반기 본전, 하반기 손실)라 봇을 늘리면 손익 폭도 같이 커진다 — 연구용 데이터로 신호·청산 개선이 먼저다.

## 11. 연구 기반 전략 개편 (research_export, 2026-01~09)

`research_export`(일봉 + 인플레이 종목 분봉)를 상반기(1~4월)/하반기(5~9월)로 나눠 검증했다. 비용은 왕복 0.23% + 슬리피지 0.1%로 잡았다.

| 셋업 | 거래당 순수익 | 비고 |
|---|---:|---|
| 기존 ORB (짧은 익절, 11시 청산) | -0.16% | 너무 일찍 청산해서 추세를 놓침 |
| 기존 종가베팅 (익일 장중까지 보유) | -0.16% | 익일 장중 보유에서 손실 |
| VWAP 눌림 / 저점매수 / 오후 돌파 / 신고가 돌파 | 음수 | 두 기간 모두 손실 |
| **ORB 필터(등락 3~10%, 갭 ≤10%, 레인지 ≤6%) + 오후까지 보유** | **+1.3~1.56%** | 두 기간 모두 양수 |
| **오버나잇: 15:15 거래대금 상위 8 매수 → 익일 시가 매도** | **+0.36%** (NXT 시가 기준, 하한) | 두 기간 모두 양수 |

반영 내용:
- **ORB 표준**: 09:05~09:30에 그날 첫 돌파만 진입. 손절 3%, 익절 10%, 15:05 청산, 부분익절·트레일링·본전이동 없음.
  - A등급(레인지 2~4%, 등락 ≤8%, 09:15 이전 돌파)이면 `ConvictionMultiplier`(1.5배)로 비중을 늘린다. 한도는 `MaxPositionAmount`×배수.
- **오버나잇 바스켓** (`EntryStrategyKind.OvernightBasket`): 15:05~15:15에 종가 후보 중 거래대금 상위 `MaxClosingBots`(8)개를 고른다.
  - 제외: 상한가 근처, +28% 이상, 고가에서 밀린 종목.
  - 자산의 `ClosingCapitalPct`(60%)를 균등하게 나눠 담고, 다음 날 시가에 매도한다.
- 기본값: `MaxDayBots` 10, `MinDayScore` 0, 일 손실 한도 3%.
- 설정 마이그레이션 v4: 기존 설정 중 예전 기본값 그대로인 항목만 새 값으로 바꾼다. 사용자가 직접 바꾼 값은 유지한다.
- 백테스트: 보유 종목의 다음 날 분봉이 없으면(export 원본) 일봉으로 합성 분봉을 만들어 청산한다. 이때 경고가 붙는다.

**주의: export 데이터 백테스트(+164%, MDD -6.9%)는 과대평가다.**
- 일봉에 NXT(08:00~20:00)가 섞여 있어, 일봉 시가가 KRX 시가와 다르다.
- 다음 날 분봉은 다음 날에도 인플레이였던 종목에만 있다. 이것이 선택 편향을 만든다.
- 분봉 유니버스 자체를 하루 전체 고가·거래대금으로 골랐다(미래 정보).

편향 없는 검증은 `--source toss` 백테스트로 해야 한다. 보유 종목의 다음 날 분봉을 직접 받아오기 때문이다. 실전 투입 전에 모의투자로도 확인한다.

## 12. 장중 프리셋 비교와 안전장치 (2026-09-26)

### 토스 데이터 백테스트 (test log4, 2026-01-01~09-25, 편향 없음)
결과는 **+130.8%, MDD −6.9%, 1,560건, PF 1.52**였다.
- 오버나잇: 1,280건, 거래당 +0.50% (상반기 +0.55%, 하반기 +0.45%).
- ORB: 280건, 거래당 +1.32% (상반기 +1.94%, 하반기 +0.45%).
  - 하반기에 A등급은 +1.04%를 유지했고, A등급이 아닌 거래는 +0.01%였다.
- export 백테스트에서 우려한 과대평가(NXT·선택 편향)는 오버나잇 거래당 약 −0.1%p 수준이었다.

### 장중 프리셋 비교 (export 데이터, 같은 조건)
| 장중 프리셋 | 전체 수익률 | MDD | 장중 거래 | 장중 거래당 |
|---|---:|---:|---:|---:|
| 사용 안 함 | +171.8% | −7.65% | — | — |
| **VWAP 추세 눌림 (신규, 기본값)** | **+208.2%** | −7.73% | 191 | **+0.72%** |
| 고가돌파 공격형 | +175.1% | −7.60% | 1,219 | −0.01% |
| VWAP 눌림 표준 | **−52.6%** | −56.4% | 2,521 | −0.40% |

### 변경
- ORB 비중 확대 조건 추가: 돌파 거래량 1.5배 이상 + 전일 종가가 20일선 위이면 1.5배.
  - 엔진이 종목별 일봉 20개를 받아 `DailyMa20`을 계산한다.
- 새 프리셋 **VWAP 추세 눌림**: 09:30~11:00, 신호 봉 거래량 2배 이상, 20일선 위, 등락 10% 미만. 손절 3%, 익절 10%, 15:05 청산.
- AutoPilot 수정:
  - 진입 시간이 끝났는데 진입하지 못한 단타 봇(09:30이 지난 ORB)은 바로 정리해 자리를 넘긴다.
  - 오전에 쓴 종목도 장중 프리셋에서 한 번 더 쓸 수 있다. 이전에는 이 두 가지 때문에 장중 프리셋이 거의 거래하지 못했다.
- 오버나잇 등락률 상한 `OvernightMaxChangePct`를 20%로 둔다. 토스 백테스트에서 +20% 이상 종목은 거래당 −0.46%, 최악 −16%였다.
- 낙폭 기반 비중 축소(낙폭 3~5%에서 절반)는 토스 결과에 적용하면 수익이 줄고 MDD는 그대로라 넣지 않았다.
- CLI 실험 옵션: `--day-preset`, `--closing off`, `--day-set`, `--morning-set`, `--autopilot-set`.
- 설정 마이그레이션 v5: 장중 프리셋이 "사용 안 함"이면 "VWAP 추세 눌림"으로 바꾼다.

## 13. 손절 거래 분석과 승률 개선 (2026-09-27)

**ORB 손절 거래(토스 42%, 연구 36%)의 특징**
- 손절을 줄이는 조건은 대부분 큰 수익도 함께 줄였다.
  - 변동성이 작은 종목은 손절 23%에 그쳤지만 거래당 +0.60%였다.
  - 시가 범위가 좁을수록 승률이 높았다.
- 시가 범위 ≤4.5%로 거르면 승률은 +2%p지만, 거래 수가 줄어 총수익이 16~23% 줄었다. 그래서 적용하지 않았다.
- 결론: 손절 3% / 익절 10% 구조에서 손절은 큰 추세를 잡기 위한 비용이다. 필터로 승률을 올리면 총수익이 줄었다.

**오버나잇에 적용한 개선**
토스 test log4에서 당일 범위 위치가 0.65 이하인 종목이 상·하반기 모두 더 좋았다.

| 범위 위치 | 승률 | 거래당 |
|---|---:|---:|
| 0.65 이하 | 56% | +0.77% |
| 0.65 초과 | 49% | +0.22% |

- 연구 데이터 바스켓에서도 거래당 +0.38% → +0.47%, 승률 55% → 57%였다.
- `OvernightMaxRangePosition = 0.65`를 추가했다.
- export 백테스트 결과: 승률 52.9% → 54.2%, PF 1.67 → 1.70, 수익률 +214.6% → +209.5%.
  - 조건을 통과하는 후보가 모자라 바스켓이 덜 차는 날이 있었다.
